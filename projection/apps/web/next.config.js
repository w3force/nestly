/** @type {import('next').NextConfig} */
const nextConfig = {
  transpilePackages: ['@projection/shared'],
  experimental: {
    // Enable other experimental features if needed
  },
};

module.exports = nextConfig;
