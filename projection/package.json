{
  "name": "projection",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter web dev",
    "dev:mobile": "pnpm --filter mobile start",
    "build": "turbo run build",
    "build:web": "pnpm --filter web build",
    "deploy:web": "cd apps/web && vercel --prod",
    "deploy:railway": "echo 'Deploy via Railway dashboard or CLI'",
    "build:ios": "cd apps/mobile && eas build --platform ios --profile preview",
    "build:android": "cd apps/mobile && eas build --platform android --profile preview",
    "build:mobile": "cd apps/mobile && eas build --platform all --profile preview"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "openapi-typescript": "^6.7.6",
    "turbo": "^1.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0"
  }
}
